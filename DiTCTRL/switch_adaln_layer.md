The adaln switch layer method is very simple. 
It dynamically changes the ADALN layer and control the transformer functionality.

Literally edits the config then loads from config with new parameters.

I am writing this because this relates to the `BaseMixin` classes in SAT.
However, the implementation of it are:
1. [[BaseAdaLNMixin]]